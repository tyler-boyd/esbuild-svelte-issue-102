{
  "name": "svelte-app",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.js",
    "dev": "WATCH=1 npm run build",
    "start": "sirv public --no-clear"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.2",
    "esbuild": "^0.14.11",
    "esbuild-svelte": "^0.6.1",
    "postcss-nested": "^5.0.6",
    "svelte": "^3.0.0",
    "svelte-preprocess": "^4.10.1",
    "tailwindcss": "^3.0.15"
  },
  "dependencies": {
    "sirv-cli": "^2.0.0"
  }
}
